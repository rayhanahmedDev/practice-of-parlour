

const Button = ({ButtonTitle}) => {
    return (
        <div>
            <button className="btn px-6 btn-error bg-[#F63E7B] text-white">{ButtonTitle}</button>
        </div>
    );
};

export default Button;